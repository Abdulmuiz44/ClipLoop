# Assets

This directory stores **text-tracked** references for design assets used in docs and product direction.

## Notes
- Binary image files were removed from git in this branch so PR tooling that only supports text diffs can run cleanly.
- If you need to add source images, upload them via your git host UI (or Git LFS) in a binary-compatible flow.

## Expected files (external or future LFS)
- ClipLoop dashboard interface mockup
- ClipLoop logo
